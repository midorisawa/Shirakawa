{
  "name": "shirakawa",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "demo": "node scripts/demo-server.mjs",
    "build": "node scripts/build.mjs",
    "package": "pnpm build && powershell -NoProfile -Command \"Compress-Archive -Path dist\\* -DestinationPath shirakawa.zip -Force\""
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.1.0"
  }
}
